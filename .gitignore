# ---- Terraform ----
# Local .terraform directories
**/.terraform/*

# Terraform state files (must never be committed)
*.tfstate
*.tfstate.*
*.tfstate.backup

# Crash log files
crash.log
crash.*.log

# Terraform plan output (often contains sensitive values)
*.tfplan
*.tfplan.*
tfplan
tfplan.*

# tfvars files often contain secrets — commit only example versions
*.tfvars
*.tfvars.json
!*.example.tfvars
!example.tfvars
!*.tfvars.example

# CLI configuration files
.terraformrc
terraform.rc

# Lock file (.terraform.lock.hcl) pins provider versions.
# Commit it in ROOT configurations (terraform/bootstrap/, terraform/environments/*/)
# but NOT in modules — modules inherit provider pins from the calling root.
terraform/modules/**/.terraform.lock.hcl

# ---- IDE / editor ----
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ---- Python (for any helper scripts / Lambda packaging) ----
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/

# ---- Node (for any tooling) ----
node_modules/

# ---- Secrets / credentials ----
# Never commit AWS credentials or .env
.env
.env.*
!.env.example
*.pem
*.key
credentials
.aws/

# ---- Build / packaging artefacts ----
dist/
build/
*.zip
*.tar.gz

# ---- Claude Code session data ----
.claude/sessions/
