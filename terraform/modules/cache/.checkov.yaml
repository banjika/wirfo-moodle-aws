---
# Cache module checkov suppressions.
#
# Inline #checkov:skip annotations are not honoured by checkov 3.2.x on Windows
# (established in T-004); this file is the operational suppression mechanism.

skip-check:
  - CKV_AWS_191   # ElastiCache replication group uses aws/elasticache default key; no CMK in Phase 1 per CLAUDE.md hard rule #3. CMK deferred to Phase 2.
  - CKV2_AWS_50   # Multi-AZ Automatic Failover disabled — Phase 1 single-AZ per CLAUDE.md hard rule #2; automatic_failover_enabled=false required when replicas_per_node_group=0. Phase 3 enables HA.
